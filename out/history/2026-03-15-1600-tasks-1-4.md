# 2026-03-15 — Tasks 1-4 complete

## Task 1 — Learner dashboard from real progress ✓
- LearnHub sectioned: up-next callout, in-progress, available, completed (with scores), locked
- nextRecommendedId computed server-side (admin nextModuleId → in-progress → first available)
- ProgressEntry enriched with score, total, lastAttemptAt

## Task 2 — Practice/quiz attempt history ✓
- practice_attempts table: append-only per attempt with full results jsonb
- Records on every checkPractice call
- GET /learn/modules/:id/attempts — learner-safe (scores only)
- GET /admin/progress/attempts?moduleId=&learnerId= — admin full detail

## Task 3 — Admin progress foundation ✓
- AdminProgressService: listLearners, getLearnerProgress, getModuleProgress
- Routes: GET /admin/progress/learners, /learners/:id, /modules/:id, /attempts
- AdminProgress.tsx: learner list + expandable detail
- Progress link in KB admin nav

## Task 4 — Group/company progress foundation ✓
- groups + group_members tables in DB
- AdminProgressService: listGroups, getGroupProgress
- Routes: GET /admin/progress/groups, /groups/:id

## Next task
Module assignment foundation (task 5): backend-first, assign modules to learners/groups
