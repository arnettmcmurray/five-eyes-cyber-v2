# 2026-03-15 — ContentPanel in ModuleManager

## What changed
- `src/pages/ModuleManager.tsx`: added `ContentPanel` component

## What it does
- Opens from "Content" button on each module row
- Loads current linked KB items (`api.modules.content`) and full item list (`api.items.list`)
- Displays: order, title/slug, type, status, role, Remove button per link
- Add form: text filter, dropdown (unlinked items only), role select, order input
- Mutations: remove via `api.lessons.remove`, add via `api.lessons.link`, reloads after each
- Typecheck passes clean

## Status
Module admin now complete: create/edit metadata, displayOrder, nextModule, prerequisites, content attachment, publish/unpublish, delete.
