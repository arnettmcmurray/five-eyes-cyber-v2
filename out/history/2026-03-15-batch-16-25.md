# Tasks 16-25: Auth, Access Control, and Contracts (2026-03-15)

## Task 16: Learner OTP Flow
- `learner_sessions` table added to auth.ts schema
- `LearnerAuthService`: `requestOtp` (logs code to stdout), `verifyOtp` (validates OTP, creates 24h session token), `validateSession`
- `/auth/otp/request` + `/auth/otp/verify` implemented (no longer 501)
- `requireLearner` switched from learnerId query param → `Authorization: Bearer <token>` header
- `/learn/identify` endpoint removed from routes + app.ts
- Frontend: `src/lib/session.ts` (token + handle storage); `OtpLoginForm` replaces `IdentifyForm` in LearnHub; api client sends learner Bearer token

## Task 17: Admin Password Auth
- `admin_users` + `admin_sessions` schema in `admin-auth.ts` (separate from learner auth)
- `AdminAuthService`: scrypt password hash (no external deps), `seedDefault`, `login`, `validateSession`, `changePassword`
- `/auth/admin/login` route implemented
- `requireAdmin` middleware at `backend/src/middleware/requireAdmin.ts`
- `/admin/*` routes all behind `requireAdmin`
- Admin seeded from `ADMIN_PASSWORD` env var at server startup
- CORS `Authorization` header added to allowed list
- Frontend: `src/lib/adminSession.ts`; `AdminLogin.tsx` at `/admin/login`; KBAdmin checks session + Log out button

## Task 18: Remove Temporary Learner Identity
- `api.learn.*` method signatures stripped of `learnerId` params
- URL query params removed from all learn API calls
- LearnModule.tsx: replaced `getLearnerId()` with `getSessionToken()`; all 5 call sites updated
- LearnHub: `api.learn.modules()` no args
- `learnerId.ts` no longer imported anywhere in frontend pages

## Task 19: Role-Based Backend Access Enforcement
- `/kb/*` routes all behind `requireAdmin`
- `/admin/*` routes all behind `requireAdmin`
- `/learn/*` routes remain behind `requireLearner`
- `api.items.*`, `api.modules.*`, `api.topics.*`, `api.lessons.*`, `api.search`, `api.ingestJobs.*`, `api.revisions.*`, `api.workflow.*` all switched to `adminReq`
- AdminProgress + AdminAssignments pages check for admin session, redirect to /admin/login

## Task 20: Admin Profile/Settings
- `GET /admin/profile` → { username }
- `POST /admin/profile/change-password` → validates + scrypt-hashes new password
- `AdminProfile.tsx` at `/admin/profile` — shows username, change-password form (clears session on change)
- Profile link added to KBAdmin nav

## Task 21: Pricing/Package Control
- `packages` schema: added `priceCents` (integer, nullable), `tier` (text), `public` (boolean) columns
- `PATCH /admin/packages/:id` route added (was missing)
- `POST /admin/packages` updated to accept new fields

## Task 22: Landing/Library/News Content Blocks
- `content_blocks` table: id, kind (landing|library-link|news), slug, title, body, linkUrl, displayOrder, published
- Full CRUD at `/admin/content-blocks` (requireAdmin)

## Tasks 23-24: Access Gating + Email Assessment Funnel Contracts
- `access_overrides` table: explicit tier overrides per learner (free|paid)
- `assessment_leads` table: email, accessToken, status, answers, followUpEnabled, followUp timestamps
- Stub routes at `/access/tier`, `/access/assessment/*`, `/admin/access/*` (all 501)
- Tier derivation logic documented in schema comment

## Task 25: TTX Branch Planning
- `out/ttx-branch-plan.md` written
- Schema design: ttx_scenarios, ttx_sessions, ttx_participants, ttx_events
- Roles: facilitator, participant, observer
- Route contract designed
- Constraint: TTX must be built in `feature/ttx-core` branch, NOT merged to main until end-to-end working

## Typechecks
- Frontend + backend: both clean throughout
